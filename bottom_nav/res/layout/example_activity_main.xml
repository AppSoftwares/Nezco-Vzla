<?xml version="1.0" encoding="utf-8"?>
<!--
  EJEMPLO DE INTEGRACIÓN
  Copia este patrón dentro de tu activity_main.xml (o el layout donde
  ya tengas tu BottomNavigationView). La idea es:
    1) Un FrameLayout de fondo con la imagen de la manguera (bg_nav_manguera)
    2) Encima, el BottomNavigationView real de Android, TRANSPARENTE,
       para que los 5 círculos de bronce de la imagen queden justo
       debajo de los 5 íconos reales y clickeables.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- ... aquí va el contenido principal de tu pantalla ... -->

    <!-- Fondo ilustrado: la manguera de incendio -->
    <ImageView
        android:id="@+id/navBackground"
        android:layout_width="match_parent"
        android:layout_height="72dp"
        android:scaleType="fitXY"
        android:src="@drawable/bg_nav_manguera"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Barra de navegación real de Android, encima, transparente -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigationView"
        android:layout_width="match_parent"
        android:layout_height="72dp"
        android:background="@android:color/transparent"
        app:itemIconTint="@color/nav_icon_tint"
        app:itemTextColor="@color/nav_text_tint"
        app:itemIconSize="26dp"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/bottom_nav_menu"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
